- hosts: all
  user: vagrant
  become: yes
  become_user: root
  become_method: sudo
  roles:
    - common
    - redis
    - db
    - web
    - docker.ubuntu
    - worker
    - webdriver
  vars_files:
    - vars/development.yml
