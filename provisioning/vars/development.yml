development: yes

# TODO: When all components are containerised, this can be the network name
redis_host: 172.18.0.1
redis_port: 6379
redis_db_number: 0
celery_broker_url: "redis://{{ redis_host }}:{{ redis_port }}/{{ redis_db_number }}"
db_password: development
admin_password: development
db_host: postgres_container
db_port: 5432
