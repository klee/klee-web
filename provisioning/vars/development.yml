development: yes

redis_host: 172.18.0.2  # replace this with kleeweb-redis once every component is dockerized
redis_port: 6379
redis_db_number: 0
celery_broker_url: "redis://{{ redis_host }}:{{ redis_port }}/{{ redis_db_number }}"
db_password: development
admin_password: development
db_host: postgres_container
db_port: 5432
