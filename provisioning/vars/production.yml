development: no

redis_host_remote: "{{ hostvars[groups['redisservers'][0]]['ansible_' + iface].ipv4.address }}"
redis_host: "127.0.0.1"
redis_port: 6379
redis_db_number: 0
celery_broker_url: "redis://{{ redis_host }}:{{ redis_port }}/{{ redis_db_number }}"

db_host: "{{ hostvars[groups['dbservers'][0]]['ansible_' + iface].ipv4.address }}"
db_port: 5432